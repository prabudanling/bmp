# Folder data berisi seluruh database JSON — TOLAK SEMUA AKSES LANGSUNG.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
